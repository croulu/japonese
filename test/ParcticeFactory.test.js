import {Kana} from "../domain/Kana";
import {PracticeFactory} from "../domain/PracticeFactory";
import {Practice} from "../domain/Practice";

import assert from "assert";
import {randomizeSyllableToGuess} from "../domain/randomizeSyllableToGuess";
import {randomizeSyllablesToPropose} from "../domain/randomizeSyllablesToPropoze";


describe('PracticeFactory', function () {
/*
    it('should provide practice from lesson in lesson catalog', function () {
        const alphabet = "h";
        const sut = new PracticeFactory();
        const syllables = [
            new Kana(alphabet, 'k', 'a'),
            new Kana(alphabet, 'k', 'i'),
            new Kana(alphabet, 'k', 'u')
        ];

        const actual = sut.createPractice(syllables);

        const expected = new Practice([
            new Kana(alphabet, 'k', 'a'),
            new Kana(alphabet, 'k', 'i'),
            new Kana(alphabet, 'k', 'u')
        ], randomizeSyllableToGuess, randomizeSyllablesToPropose());
        assert.deepEqual(actual, expected);
    });

 */
});


