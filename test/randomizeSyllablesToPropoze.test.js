import assert from "assert";
import {randomizeSyllablesToPropoze} from "../domain/randomizeSyllablesToPropoze";


describe('randomizeSyllablesToPropoze', function () {
    /*
    it('should shuffle all provided syllables', function () {
        const list = [7, 8, 9];
        const hundredElementArray = [...Array(100).keys()];
        const result = hundredElementArray.map(() => randomizeSyllablesToPropose(list));
        assert.ok(list.every(k => result.includes(k)))
    })

     */
})
